# Sito vetrina — Pierporz

Pagina singola statica: per ora solo nome e recapiti (finti, da sostituire).
Nessuna dipendenza, nessun build.

```
index.html            la pagina
assets/css/style.css  lo stile
```

## Anteprima locale

```bash
python3 -m http.server 8000
# poi apri http://localhost:8000
```

## Prima di pubblicare

Sostituire in `index.html` email (`ciao@pierporz.it`) e telefono (`+39 000 000 0000`).
